#####################################################################
### Aeotec MultiSensor 6 Motion Detector status
#####################################################################
- platform: template
  sensors:
    motionsensor:
      value_template: >-
        {% set state = states('sensor.living_room_multisensor_burglar') %}
        {% if state == '8' %}
          Motion
        {% elif state == '0' %}
          Idle
        {% elif state == '3' %}
          Tamper
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Living Room Motion Sensor'

#####################################################################
### Aeotec Door/Window Sensor 6 Front Door status
#####################################################################
- platform: template
  sensors:
    frontdoorsensor:
      value_template: >-
        {% set state = states('binary_sensor.front_door_sensor') %}
        {% if state == 'on' %}
          Open
        {% elif state == 'off' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Front Door Sensor'

#####################################################################
### Aeotec Door/Window Sensor 6 Garage Door status
#####################################################################
- platform: template
  sensors:
    garagedoorsensor:
      value_template: >-
        {% set state = states('binary_sensor.garage_door_sensor') %}
        {% if state == 'on' %}
          Open
        {% elif state == 'off' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Garage Door Sensor'

- platform: template
  sensors:
    denwindowsensor:
      value_template: >-
        {% set state = states('sensor.den_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Den Window Sensor'

- platform: template
  sensors:
    jaydenwindowsensor:
      value_template: >-
        {% set state = states('sensor.jayden_room_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Jayden Window Sensor'

- platform: template
  sensors:
    backdoorsensor:
      value_template: >-
        {% set state = states('sensor.back_door_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Back Door Sensor'

- platform: template
  sensors:
    alliewindowsensor:
      value_template: >-
        {% set state = states('sensor.allie_room_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Allie Window Sensor'


- platform: template
  sensors:
    anthony_location:
      value_template: >-
        {% set state = states('device_tracker.anthonys_iphone') %}
        {% if state == 'home' %}
          home
        {% elif state == 'near_home' %}
          home
        {% else %}
          away
        {% endif %}
      friendly_name: 'Anthony'

- platform: template
  sensors:
    tawny_location:
      value_template: >-
        {% set state = states('device_tracker.tawnys_iphone') %}
        {% if state == 'home' %}
          home
        {% elif state == 'near_home' %}
          home
        {% else %}
          away
        {% endif %}
      friendly_name: 'Tawny'

- platform: template
  sensors:
    bonus_room_north_window:
      value_template: >-
        {% set state = states('sensor.bonus_room_north_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Bonus Room N Window Sensor'

- platform: template
  sensors:
    bonus_room_ne_window:
      value_template: >-
        {% set state = states('sensor.bonus_room_ne_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Bonus Room NE Window Sensor'

- platform: template
  sensors:
    bonus_room_se_window:
      value_template: >-
        {% set state = states('sensor.bonus_room_se_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Bonus Room SE Window Sensor'

- platform: template
  sensors:
    laundry_room_window:
      value_template: >-
        {% set state = states('sensor.laundry_room_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Laundry Room Window Sensor'

- platform: template
  sensors:
    dining_room_n_window:
      value_template: >-
        {% set state = states('sensor.dining_room_n_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Dining Room N Window Sensor'

- platform: template
  sensors:
    dining_room_s_window:
      value_template: >-
        {% set state = states('sensor.dining_room_s_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Dining Room S Window Sensor'

- platform: template
  sensors:
    living_room_e_window:
      value_template: >-
        {% set state = states('sensor.living_room_e_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Living Room E Window Sensor'

- platform: template
  sensors:
    living_room_se_window:
      value_template: >-
        {% set state = states('sensor.living_room_se_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Living Room SE Window Sensor'

- platform: template
  sensors:
    living_room_sw_window:
      value_template: >-
        {% set state = states('sensor.living_room_sw_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Living Room SW Window Sensor'

- platform: template
  sensors:
    master_bedroom_se_window:
      value_template: >-
        {% set state = states('sensor.master_bedroom_se_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Master Bedroom SE Window Sensor'

- platform: template
  sensors:
    master_bedroom_sw_window:
      value_template: >-
        {% set state = states('sensor.master_bedroom_sw_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Master Bedroom SW Window Sensor'

- platform: template
  sensors:
    master_bedroom_w_window:
      value_template: >-
        {% set state = states('sensor.master_bedroom_w_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Master Bedroom W Window Sensor'

- platform: template
  sensors:
    hallway_window:
      value_template: >-
        {% set state = states('sensor.hallway_window_access_control') %}
        {% if state == '22' %}
          Open
        {% elif state == '23' %}
          Closed
        {% else %}
          {{ state }}
        {% endif %}
      friendly_name: 'Hallway Window Sensor'




